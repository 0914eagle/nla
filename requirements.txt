# NLA x Transcoder pilot runtime.
#
# Verified constraints:
# - Python >=3.10 is required by circuit-tracer.
# - circuit-tracer 0.4.1 requires transformers >=4.56.0, <=4.57.3 and nnsight==0.6.1.
# - nla-inference is not pip-installable at commit 38b802a because it has no
#   pyproject.toml/setup.py. Its single-file client is vendored as nla_inference.py.
#   That client needs torch/transformers/safetensors/httpx/orjson/PyYAML/numpy.
# - sglang is intentionally not installed here because newer safe SGLang releases
#   require transformers 5.x, which conflicts with circuit-tracer. See
#   requirements-sglang-server.txt if you want to run AV serving in a separate env.

--index-url https://download.pytorch.org/whl/cu124
--extra-index-url https://pypi.org/simple

torch==2.6.0+cu124
accelerate==1.12.0
transformers==4.57.1
tokenizers==0.22.1
huggingface-hub==0.36.0
safetensors==0.7.0

numpy==1.26.4
PyYAML==6.0.3
httpx==0.28.1
orjson==3.11.5
pydantic==2.12.5
tqdm==4.67.1
einops==0.8.1

nnsight==0.6.1
transformer-lens==2.16.1
seaborn==0.13.2
ipykernel==6.30.1
ipywidgets==8.1.8

circuit-tracer @ git+https://github.com/decoderesearch/circuit-tracer.git@041a9b2cbd7f3fe7e0a625a6794e66fc4aa5f883
