# Optional separate environment for the NLA AV SGLang server.
#
# Keep this separate from requirements.txt:
# - sglang==0.5.6 pins transformers==4.57.1, which is compatible with circuit-tracer.
# - sglang>=0.5.10 fixes several advisories but currently pins transformers==5.3.0,
#   which conflicts with circuit-tracer<=0.4.1.
# - Do not expose this server to the public internet. Bind it to localhost or a
#   private interface and run with --disable-radix-cache as required by nla-inference.

# Use the default PyPI index for this file. sglang==0.5.6 pins torch==2.9.1,
# and PyTorch 2.9 wheels are not published for the old cu121 local-version line.
sglang==0.5.6
transformers==4.57.1
torch==2.9.1
torchaudio==2.9.1
torchvision==0.24.1
numpy==2.3.5
orjson==3.11.5
httpx==0.28.1
safetensors==0.7.0
PyYAML==6.0.3
